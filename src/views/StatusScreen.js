import React from 'react';
import { View, Text } from 'react-native';

const StatusScreen = () => {
  return (
    <View className="flex-1 flex justify-center items-center">
      <Text className="text-xl font-semibold">Status Screen</Text>
    </View>
  );
};

export default StatusScreen;
